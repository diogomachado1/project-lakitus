name: Update-game-stack

on:
  push:
    branches: [main]
    paths: 
      - '.github/workflows/cd-game-service.yaml'
      - 'config/game.yml'

jobs:
  update-stack:
    runs-on: ubuntu-latest
    steps:
    - name: Webhook
      uses: distributhor/workflow-webhook@v2
      env:
        webhook_url: "https://portainer.leari.xyz/api/stacks/webhooks/11c1cb03-3c69-4bd6-abd9-c1a53e3fac4b"
        webhook_secret: "Y0uR5ecr3t"

      
          